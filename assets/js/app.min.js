const state={primary:"#3B82F6",lightReach:96,darkReach:24,depth:8,theme:"light",format:"css"};const derivedRoleScales=[{name:"primary",hueOffset:0,saturationOffset:0},{name:"secondary",hueOffset:36,saturationOffset:-4},{name:"accent",hueOffset:-42,saturationOffset:5},{name:"note",hueOffset:18,saturationOffset:-2}];const scaleRoleLinks=[{name:"info",scale:"grey",stop:300},{name:"success",scale:"green",stop:300},{name:"warning",scale:"orange",stop:300},{name:"error",scale:"red",stop:300}];const spectrumScales=[{name:"yellow",hue:60},{name:"orange",hue:32},{name:"red",hue:0},{name:"magenta",hue:300},{name:"violet",hue:258},{name:"blue",hue:230},{name:"cyan",hue:190},{name:"green",hue:128},{name:"grey",hue:220,neutral:true}];const depthStops={7:[50,100,200,300,400,500,600],8:[50,100,200,300,400,500,600,700],10:[50,100,200,300,400,500,600,700,800,900]};const themeTokens={light:{background:"#f8fafc",surface:"#ffffff",surfaceRaised:"#fdfefe",border:"#dbe3ee",text:"#172033",muted:"#66758a",codeBg:"#111827",codeText:"#dbeafe"},dark:{background:"#0f172a",surface:"#172033",surfaceRaised:"#1d2738",border:"#324155",text:"#e5edf7",muted:"#9fb0c5",codeBg:"#07111f",codeText:"#d8e7ff"}};const picker=document.querySelector("#colorPicker");const hexInput=document.querySelector("#hexInput");const hexStatus=document.querySelector("#hexStatus");const lightnessRange=document.querySelector("#lightnessRange");const darknessRange=document.querySelector("#darknessRange");const lightnessOutput=document.querySelector("#lightnessOutput");const darknessOutput=document.querySelector("#darknessOutput");const themeToggle=document.querySelector("#themeToggle");const themeToggleLabel=document.querySelector("#themeToggleLabel");const toneStrip=document.querySelector("#toneStrip");const roleStrip=document.querySelector("#roleStrip");const spectrumGrid=document.querySelector("#spectrumGrid");const copyMenu=document.querySelector("#copyMenu");const exportPanel=document.querySelector("#panel-export");const exportLabel=document.querySelector("#exportLabel");const exportText=document.querySelector("#exportText");const downloadButton=document.querySelector("#downloadButton");const copyButton=document.querySelector("#copyButton");const copyStatus=document.querySelector("#copyStatus");const tokenCount=document.querySelector("#tokenCount");const tabButtons=[...document.querySelectorAll(".tab-button")];let activeCopyTarget=null;let activeCopyAnchor=null;let generatedId=0;function nextGeneratedId(prefix){generatedId+=1;return`${prefix}-${generatedId}`;}function getAppStylesheet(){return[...document.styleSheets].find((sheet)=>sheet.href&&sheet.href.endsWith("styles.css"))||document.styleSheets[0];}function removeDynamicRules(match){const sheet=getAppStylesheet();if(!sheet)return;for(let index=sheet.cssRules.length-1;index>=0;index-=1){const rule=sheet.cssRules[index];if(rule.selectorText&&match(rule.selectorText))sheet.deleteRule(index);}}function setDynamicRule(selector,declarations){const sheet=getAppStylesheet();if(!sheet)return;removeDynamicRules((selectorText)=>selectorText===selector);sheet.insertRule(`${selector} { ${declarations} }`,sheet.cssRules.length);}function clearGeneratedElementRules(){removeDynamicRules((selectorText)=>selectorText.startsWith('[data-tone-id=')||selectorText.startsWith('[data-role-id=')||selectorText.startsWith('[data-list-id=')||selectorText.startsWith('[data-swatch-id='));}function removeGeneratedRule(selector){removeDynamicRules((selectorText)=>selectorText===selector);}function clamp(value,min,max){return Math.min(max,Math.max(min,value));}function normalizeHex(value){const trimmed=value.trim();const withHash=trimmed.startsWith("#")?trimmed:`#${trimmed}`;if(/^#[0-9a-fA-F]{3}$/.test(withHash)){return`#${withHash[1]}${withHash[1]}${withHash[2]}${withHash[2]}${withHash[3]}${withHash[3]}`.toUpperCase();}if(/^#[0-9a-fA-F]{6}$/.test(withHash)){return withHash.toUpperCase();}return null;}function hexToRgb(hex){const value=normalizeHex(hex).slice(1);return{r:parseInt(value.slice(0,2),16),g:parseInt(value.slice(2,4),16),b:parseInt(value.slice(4,6),16)};}function rgbToHex({r,g,b}){return`#${[r, g, b].map((channel) => clamp(Math.round(channel), 0, 255).toString(16).padStart(2, "0")).join("")}`.toUpperCase();}function rgbToHsl({r,g,b}){const red=r/255;const green=g/255;const blue=b/255;const max=Math.max(red,green,blue);const min=Math.min(red,green,blue);const lightness=(max+min)/2;const delta=max-min;let hue=0;let saturation=0;if(delta!==0){saturation=delta/(1-Math.abs(2*lightness-1));if(max===red)hue=60*(((green-blue)/delta)%6);if(max===green)hue=60*((blue-red)/delta+2);if(max===blue)hue=60*((red-green)/delta+4);}return{h:(hue+360)%360,s:saturation*100,l:lightness*100};}function hslToRgb({h,s,l}){const saturation=s/100;const lightness=l/100;const chroma=(1-Math.abs(2*lightness-1))*saturation;const huePrime=h/60;const x=chroma*(1-Math.abs((huePrime%2)-1));let red=0;let green=0;let blue=0;if(huePrime>=0&&huePrime<1)[red,green,blue]=[chroma,x,0];else if(huePrime<2)[red,green,blue]=[x,chroma,0];else if(huePrime<3)[red,green,blue]=[0,chroma,x];else if(huePrime<4)[red,green,blue]=[0,x,chroma];else if(huePrime<5)[red,green,blue]=[x,0,chroma];else[red,green,blue]=[chroma,0,x];const match=lightness-chroma/2;return{r:(red+match)*255,g:(green+match)*255,b:(blue+match)*255};}function getRelativeLuminance(hex){const{r,g,b}=hexToRgb(hex);const channels=[r,g,b].map((channel)=>{const value=channel/255;return value<=0.03928?value/12.92:((value+0.055)/1.055)**2.4;});return 0.2126*channels[0]+0.7152*channels[1]+0.0722*channels[2];}function getContrastRatio(first,second){const firstLuminance=getRelativeLuminance(first);const secondLuminance=getRelativeLuminance(second);return(Math.max(firstLuminance,secondLuminance)+0.05)/(Math.min(firstLuminance,secondLuminance)+0.05);}function getReadableInk(hex){return getContrastRatio(hex,"#000000")>=getContrastRatio(hex,"#FFFFFF")?"#000000":"#FFFFFF";}function mixLightness(index,total,lightReach,darkReach){if(total===1)return 50;const ratio=index/(total-1);return lightReach+(darkReach-lightReach)*ratio;}function findSpectrumToken(spectrumData,scaleName,stop){const scale=spectrumData.find((item)=>item.name===scaleName);if(!scale)return null;return scale.tokens.find((token)=>token.stop===stop)||null;}function buildRoleData(mode,spectrumData){const primaryHsl=rgbToHsl(hexToRgb(state.primary));const derivedRoles=derivedRoleScales.map((role)=>{const hue=(primaryHsl.h+role.hueOffset+360)%360;const saturation=clamp(primaryHsl.s+role.saturationOffset,42,94);const baseLightness=clamp(primaryHsl.l,state.darkReach,state.lightReach);const lightness=mode==="dark"?clamp(baseLightness-18,36,64):baseLightness;const hex=rgbToHex(hslToRgb({h:hue,s:saturation,l:lightness}));return{name:role.name,hex};});const linkedRoles=scaleRoleLinks.map((role)=>{const token=findSpectrumToken(spectrumData,role.scale,role.stop);return{name:role.name,hex:token?token.hex:state.primary,source:token?token.name:null};});return[...derivedRoles,...linkedRoles];}function buildSpectrumData(mode){const primaryHsl=rgbToHsl(hexToRgb(state.primary));const stops=depthStops[state.depth];const baseSaturation=clamp(primaryHsl.s,42,94);const lightRange=mode==="dark"?{light:clamp(state.lightReach-38,42,62),dark:clamp(state.darkReach-14,6,18)}:{light:state.lightReach,dark:state.darkReach};return spectrumScales.map((scale)=>{const tokens=stops.map((stop,index)=>{const lightness=mixLightness(index,stops.length,lightRange.light,lightRange.dark);const saturation=scale.neutral?clamp(primaryHsl.s*0.08,4,12):clamp(baseSaturation-Math.max(0,index-4)*2.4,34,94);const hue=scale.neutral?primaryHsl.h:scale.hue;const hex=rgbToHex(hslToRgb({h:hue,s:saturation,l:lightness}));return{name:`${scale.name}-${stop}`,stop,hex};});return{name:scale.name,tokens};});}function getFlatSpectrumTokens(data){return data.flatMap((scale)=>scale.tokens.map((token)=>({...token,scale:scale.name})));}function roleMap(roleData){return Object.fromEntries(roleData.map((role)=>[role.name,role.hex]));}function spectrumMap(spectrumData){const colors={};spectrumData.forEach((scale)=>{colors[scale.name]={};scale.tokens.forEach((token)=>{colors[scale.name][token.stop]=token.hex;});});return colors;}function formatExportLabel(value){return value.split("-").map((part)=>part.charAt(0).toUpperCase()+part.slice(1)).join(" ");}function appendCssTheme(lines,selector,roleData,spectrumData,mode){const theme=themeTokens[mode];lines.push(`/* ==================== ${formatExportLabel(mode)} Theme ==================== */`);lines.push(`${selector} {`);if(selector===":root")lines.push(`  color-scheme: light;`);if(selector===".dark")lines.push(`  color-scheme: dark;`);lines.push("","  /* Role tokens */");roleData.forEach((role)=>lines.push(`  --${role.name}: ${role.hex};`));lines.push("","  /* Spectrum tokens */");spectrumData.forEach((scale)=>{lines.push("",`  /* ${formatExportLabel(scale.name)} */`);scale.tokens.forEach((token)=>lines.push(`  --${token.name}: ${token.hex};`));});lines.push("","  /* Semantic aliases */");lines.push(`  --color-primary: var(--primary);`);lines.push(`  --color-primary-hover: var(--primary);`);lines.push(`  --color-secondary: var(--secondary);`);lines.push(`  --color-accent: var(--accent);`);lines.push(`  --color-note: var(--note);`);lines.push(`  --color-info: var(--info);`);lines.push(`  --color-success: var(--success);`);lines.push(`  --color-warning: var(--warning);`);lines.push(`  --color-error: var(--error);`);spectrumData.forEach((scale)=>lines.push(`  --color-${scale.name}: var(--${scale.name}-500);`));lines.push("","  /* Surface tokens */");lines.push(`  --color-background: ${theme.background};`);lines.push(`  --color-surface: ${theme.surface};`);lines.push(`  --color-surface-raised: ${theme.surfaceRaised};`);lines.push(`  --color-border: ${theme.border};`);lines.push(`  --color-text: ${theme.text};`);lines.push(`  --color-muted: ${theme.muted};`);lines.push("}");}function buildCss(tokenSets){const lines=["/* Add class=\"dark\" to a parent element to load the dark token values. */","/* CSS uses block comments so this export remains valid global CSS. */",""];appendCssTheme(lines,":root",tokenSets.light.roles,tokenSets.light.spectrum,"light");lines.push("");appendCssTheme(lines,".dark",tokenSets.dark.roles,tokenSets.dark.spectrum,"dark");return lines.join("\n");}function appendJsObjectEntries(lines,entries,indent){entries.forEach(([key,value])=>lines.push(`${indent}${key}: "${value}",`));}function buildJs(tokenSets){const lines=["// ==================== Role Colors ====================","export const roleColors = {"];["light","dark"].forEach((mode)=>{lines.push(`  // ${formatExportLabel(mode)} theme`);lines.push(`  ${mode}: {`);appendJsObjectEntries(lines,Object.entries(roleMap(tokenSets[mode].roles)),"    ");lines.push("  },");});lines.push("};","","// ==================== Spectrum Tokens ====================","export const colorTokens = {");["light","dark"].forEach((mode)=>{lines.push(`  // ${formatExportLabel(mode)} theme`);lines.push(`  ${mode}: {`);tokenSets[mode].spectrum.forEach((scale)=>{lines.push(`    // ${formatExportLabel(scale.name)}`);lines.push(`    ${scale.name}: {`);scale.tokens.forEach((token)=>lines.push(`      ${token.stop}: "${token.hex}",`));lines.push("    },");});lines.push("  },");});lines.push("};","","// ==================== Semantic Colors ====================","export const semanticColors = {");["light","dark"].forEach((mode)=>{const roles=roleMap(tokenSets[mode].roles);const theme=themeTokens[mode];lines.push(`  // ${formatExportLabel(mode)} theme`);lines.push(`  ${mode}: {`);["primary","secondary","accent","note","info","success","warning","error"].forEach((role)=>{lines.push(`    ${role}: "${roles[role]}",`);});lines.push(`    background: "${theme.background}",`);lines.push(`    surface: "${theme.surface}",`);lines.push(`    border: "${theme.border}",`);lines.push(`    text: "${theme.text}",`);lines.push("  },");});lines.push("};");return lines.join("\n");}function buildFigma(tokenSets){const figma={$description:"Palette export organized by light and dark theme modes.",modes:{light:{$description:"Light theme tokens.",color:{role:{},spectrum:{},semantic:themeTokens.light}},dark:{$description:"Dark theme tokens for a parent .dark class pattern.",color:{role:{},spectrum:{},semantic:themeTokens.dark}}}};["light","dark"].forEach((mode)=>{tokenSets[mode].roles.forEach((role)=>{figma.modes[mode].color.role[role.name]={value:role.hex,type:"color",description:`${formatExportLabel(mode)} ${formatExportLabel(role.name)} role`};});tokenSets[mode].spectrum.forEach((scale)=>{figma.modes[mode].color.spectrum[scale.name]={$description:`${formatExportLabel(mode)} ${formatExportLabel(scale.name)} scale`};scale.tokens.forEach((token)=>{figma.modes[mode].color.spectrum[scale.name][token.stop]={value:token.hex,type:"color",description:token.name};});});});return JSON.stringify(figma,null,2);}function buildJson(tokenSets){const tokens={$description:"Production color tokens organized by mode, role, spectrum, and semantic surface values.",color:{modes:{light:{$description:"Light theme tokens.",role:{},spectrum:{},semantic:themeTokens.light},dark:{$description:"Dark theme tokens for parent .dark class usage.",role:{},spectrum:{},semantic:themeTokens.dark}}}};["light","dark"].forEach((mode)=>{tokenSets[mode].roles.forEach((role)=>{tokens.color.modes[mode].role[role.name]={value:role.hex,type:"color",name:role.name,description:`${formatExportLabel(mode)} ${formatExportLabel(role.name)} role`};});tokenSets[mode].spectrum.forEach((scale)=>{tokens.color.modes[mode].spectrum[scale.name]={$description:`${formatExportLabel(mode)} ${formatExportLabel(scale.name)} scale`};scale.tokens.forEach((token)=>{tokens.color.modes[mode].spectrum[scale.name][token.stop]={value:token.hex,type:"color",name:token.name,description:`${formatExportLabel(scale.name)} ${token.stop}`};});});});return JSON.stringify(tokens,null,2);}function buildTailwind(tokenSets){const lines=["// Use Tailwind's class dark mode with `darkMode: 'class'` and add class=\"dark\" on a parent.","export default {","  darkMode: 'class',","  theme: {","    extend: {","      colors: {"];["light","dark"].forEach((mode)=>{lines.push(`        // ==================== ${formatExportLabel(mode)} Theme ====================`);lines.push(`        "${mode}": {`);lines.push("          // Roles");tokenSets[mode].roles.forEach((role)=>lines.push(`          "${role.name}": "${role.hex}",`));lines.push("          // Spectrum");tokenSets[mode].spectrum.forEach((scale)=>{lines.push(`          // ${formatExportLabel(scale.name)}`);lines.push(`          "${scale.name}": {`);scale.tokens.forEach((token)=>lines.push(`            ${token.stop}: "${token.hex}",`));lines.push("          },");});lines.push("        },");});lines.push("      }","    }","  }","};");return lines.join("\n");}async function copyText(value,event){try{await navigator.clipboard.writeText(value);}catch{const previous=exportText.value;exportText.value=value;exportText.select();document.execCommand("copy");exportText.value=previous;}copyStatus.textContent=`Copied ${value}`;showCopiedBurst(event);window.setTimeout(()=>{copyStatus.textContent="Ready to copy";},1400);}function showCopiedBurst(event){if(!event)return;const burst=document.createElement("span");burst.className="copied-burst";burst.textContent="Copied!";const burstId=nextGeneratedId("copied-burst");const selector=`[data-burst-id="${burstId}"]`;burst.dataset.burstId=burstId;setDynamicRule(selector,`left: ${event.clientX}px; top: ${event.clientY}px;`);document.body.append(burst);burst.addEventListener("animationend",()=>{burst.remove();removeGeneratedRule(selector);},{once:true});}function getCopyValue(target,type){if(!target)return"";if(type==="token")return target.token;if(type==="hex")return target.hex;if(type==="var")return`var(--${target.token})`;return`--${target.token}: ${target.hex};`;}function openCopyMenu(target,anchor){activeCopyTarget=target;activeCopyAnchor=anchor;const rect=anchor.getBoundingClientRect();copyMenu.hidden=false;const menuLeft=Math.min(Math.max(rect.left,8),window.innerWidth-190);const pointerLeft=Math.max(18,Math.min(rect.left+rect.width/2-menuLeft,160));setDynamicRule("#copyMenu",`left: ${menuLeft}px; top: ${rect.bottom - 6}px; --pointer-left: ${pointerLeft}px;`);copyMenu.querySelector("button")?.focus();}function closeCopyMenu({restoreFocus=false}={}){copyMenu.hidden=true;activeCopyTarget=null;if(restoreFocus)activeCopyAnchor?.focus();activeCopyAnchor=null;}function renderToneStrip(spectrumData){toneStrip.innerHTML="";spectrumData.filter((scale)=>!scale.tokens.some((token)=>token.name.startsWith("grey-"))).forEach((scale)=>{const token=scale.tokens.find((item)=>item.stop===300)||scale.tokens[Math.floor(scale.tokens.length/2)];const segment=document.createElement("button");segment.type="button";const toneId=nextGeneratedId("tone");segment.className="tone-strip-segment";segment.dataset.toneId=toneId;setDynamicRule(`[data-tone-id="${toneId}"]`,`--tone-color: ${token.hex};`);segment.setAttribute("aria-label",`Copy ${token.hex} for ${token.name}`);segment.addEventListener("click",(event)=>copyText(token.hex,event));toneStrip.append(segment);});}function renderRoles(roleData){roleStrip.innerHTML="";roleData.forEach((role)=>{const swatch=document.createElement("button");swatch.type="button";const roleId=nextGeneratedId("role");swatch.className="role-swatch";swatch.dataset.roleId=roleId;setDynamicRule(`[data-role-id="${roleId}"]`,`--role-color: ${role.hex}; --role-ink: ${getReadableInk(role.hex)};`);swatch.setAttribute("aria-label",`${formatExportLabel(role.name)} ${role.hex}. Open copy options`);swatch.innerHTML=`<span aria-hidden="true"></span><strong>${role.name}</strong><small>${role.hex}</small>`;swatch.addEventListener("click",(event)=>{event.stopPropagation();openCopyMenu({token:role.name,hex:role.hex},swatch);});roleStrip.append(swatch);});}function renderSpectrum(spectrumData){spectrumGrid.innerHTML="";setDynamicRule("#spectrumGrid",`--column-count: ${depthStops[state.depth].length};`);spectrumData.forEach((scale)=>{const row=document.createElement("article");row.className="scale-row";const swatches=document.createElement("div");const listId=nextGeneratedId("scale-list");swatches.className="swatch-list";swatches.dataset.listId=listId;setDynamicRule(`[data-list-id="${listId}"]`,`--scale-column-count: ${scale.tokens.length};`);scale.tokens.forEach((token)=>{const swatch=document.createElement("button");swatch.type="button";const swatchId=nextGeneratedId("swatch");swatch.className="swatch";swatch.dataset.swatchId=swatchId;setDynamicRule(`[data-swatch-id="${swatchId}"]`,`--swatch-color: ${token.hex}; --swatch-ink: ${getReadableInk(token.hex)};`);swatch.setAttribute("aria-label",`${token.hex}. Open copy options for ${token.name}`);swatch.innerHTML=`<span>${token.hex}</span>`;swatch.addEventListener("click",(event)=>{event.stopPropagation();openCopyMenu({token:token.name,hex:token.hex},swatch);});swatches.append(swatch);});row.append(swatches);spectrumGrid.append(row);});}function getExportFileMeta(format){const meta={css:{filename:"spectrum-tokens.css",type:"text/css"},js:{filename:"spectrum-tokens.js",type:"text/javascript"},figma:{filename:"spectrum-tokens.figma.json",type:"application/json"},json:{filename:"spectrum-tokens.tokens.json",type:"application/json"},tailwind:{filename:"tailwind.palette.config.js",type:"text/javascript"}};return meta[format]||{filename:"spectrum-tokens.txt",type:"text/plain"};}function downloadExport(){const{filename,type}=getExportFileMeta(state.format);const blob=new Blob([exportText.value],{type:`${type};charset=utf-8`});const url=URL.createObjectURL(blob);const link=document.createElement("a");link.href=url;link.download=filename;document.body.append(link);link.click();link.remove();URL.revokeObjectURL(url);copyStatus.textContent=`Downloaded ${filename}`;window.setTimeout(()=>{copyStatus.textContent="Ready to copy";},1800);}function renderExports(tokenSets){const exports={css:buildCss(tokenSets),js:buildJs(tokenSets),figma:buildFigma(tokenSets),json:buildJson(tokenSets),tailwind:buildTailwind(tokenSets)};const labels={css:"Global CSS output",js:"JavaScript output",figma:"Figma tokens output",json:"Tokens JSON output",tailwind:"Tailwind config output"};exportLabel.textContent=labels[state.format];exportText.value=exports[state.format];}function applyThemeToPage(){document.documentElement.classList.toggle("dark",state.theme==="dark");document.documentElement.dataset.generatedPreview=state.theme;themeToggle.setAttribute("aria-pressed",String(state.theme==="dark"));themeToggleLabel.textContent=state.theme==="dark"?"Dark":"Light";}function updatePreview(roleData,spectrumData){const roles=roleMap(roleData);const byName=Object.fromEntries(getFlatSpectrumTokens(spectrumData).map((token)=>[token.name,token.hex]));const theme=themeTokens[state.theme];const previewPrimary=roles.primary||state.primary;setDynamicRule("html[data-generated-preview]",[`--primary: ${previewPrimary}`,`--primary-soft: ${byName["violet-100"] || previewPrimary}`,`--primary-deep: ${byName["violet-600"] || previewPrimary}`,`--focus: ${previewPrimary}`,`--preview-bg: ${byName["violet-100"] || theme.surfaceRaised}`,`--preview-border: ${byName["violet-200"] || theme.border}`,`--preview-primary: ${previewPrimary}`,`--preview-on-primary: ${getReadableInk(previewPrimary)}`,`--preview-text: ${theme.text}`].join("; "));}function buildTokenSets(){const lightSpectrum=buildSpectrumData("light");const darkSpectrum=buildSpectrumData("dark");return{light:{roles:buildRoleData("light",lightSpectrum),spectrum:lightSpectrum},dark:{roles:buildRoleData("dark",darkSpectrum),spectrum:darkSpectrum}};}function render(){lightnessOutput.value=`${state.lightReach}%`;darknessOutput.value=`${state.darkReach}%`;picker.value=state.primary;hexInput.value=state.primary;hexStatus.textContent="Valid hex color";hexStatus.classList.remove("error");applyThemeToPage();clearGeneratedElementRules();const tokenSets=buildTokenSets();const activeTokens=tokenSets[state.theme];const count=activeTokens.spectrum.reduce((total,scale)=>total+scale.tokens.length,0);tokenCount.textContent=`${count} ${state.theme} color tokens`;renderToneStrip(activeTokens.spectrum);renderRoles(activeTokens.roles);renderSpectrum(activeTokens.spectrum);renderExports(tokenSets);updatePreview(activeTokens.roles,activeTokens.spectrum);}picker.addEventListener("input",(event)=>{state.primary=event.target.value.toUpperCase();render();});hexInput.addEventListener("input",(event)=>{const normalized=normalizeHex(event.target.value);if(!normalized){hexStatus.textContent="Enter a valid 3 or 6 digit hex value";hexStatus.classList.add("error");return;}state.primary=normalized;render();});lightnessRange.addEventListener("input",(event)=>{state.lightReach=Number(event.target.value);render();});darknessRange.addEventListener("input",(event)=>{state.darkReach=Number(event.target.value);render();});themeToggle.addEventListener("click",()=>{state.theme=state.theme==="dark"?"light":"dark";render();});document.querySelectorAll("input[name='depth']").forEach((input)=>{input.addEventListener("change",(event)=>{state.depth=Number(event.target.value);render();});});copyMenu.addEventListener("click",(event)=>{const button=event.target.closest("button[data-copy]");if(!button)return;copyText(getCopyValue(activeCopyTarget,button.dataset.copy),event);closeCopyMenu();});document.addEventListener("click",(event)=>{if(!copyMenu.hidden&&!copyMenu.contains(event.target))closeCopyMenu();});document.addEventListener("keydown",(event)=>{if(event.key==="Escape")closeCopyMenu({restoreFocus:true});});copyMenu.addEventListener("keydown",(event)=>{const items=[...copyMenu.querySelectorAll("button")];const currentIndex=items.indexOf(document.activeElement);if(event.key==="ArrowDown"||event.key==="ArrowRight"){event.preventDefault();items[(currentIndex+1+items.length)%items.length]?.focus();}if(event.key==="ArrowUp"||event.key==="ArrowLeft"){event.preventDefault();items[(currentIndex-1+items.length)%items.length]?.focus();}});window.addEventListener("scroll",closeCopyMenu,{passive:true,capture:true});document.addEventListener("scroll",closeCopyMenu,{passive:true,capture:true});function activateExportTab(button){state.format=button.dataset.format;tabButtons.forEach((tab)=>{const selected=tab===button;tab.setAttribute("aria-selected",String(selected));tab.tabIndex=selected?0:-1;});exportPanel.setAttribute("aria-labelledby",button.id);render();}tabButtons.forEach((button,index)=>{button.tabIndex=button.getAttribute("aria-selected")==="true"?0:-1;button.addEventListener("click",()=>activateExportTab(button));button.addEventListener("keydown",(event)=>{const offset=event.key==="ArrowRight"?1:event.key==="ArrowLeft"?-1:0;if(!offset)return;event.preventDefault();const next=tabButtons[(index+offset+tabButtons.length)%tabButtons.length];next.focus();activateExportTab(next);});});downloadButton.addEventListener("click",downloadExport);document.querySelector("#controls").addEventListener("submit",(event)=>event.preventDefault());copyButton.addEventListener("click",async(event)=>{try{await navigator.clipboard.writeText(exportText.value);copyStatus.textContent="Copied";}catch{exportText.select();document.execCommand("copy");copyStatus.textContent="Copied";}showCopiedBurst(event);window.setTimeout(()=>{copyStatus.textContent="Ready to copy";},1600);});if("serviceWorker"in navigator){window.addEventListener("load",()=>{navigator.serviceWorker.register("./service-worker.js").catch(()=>{});});}render();
